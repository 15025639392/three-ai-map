# Three.js 地球引擎第六阶段验收

## 启动

```bash
npm install
npm run dev
```

打开浏览器访问默认开发地址。

## 验收项

- 页面加载后可看到一个可旋转、可缩放的地球
- 初始视角定位在亚洲区域附近
- 地球表面默认加载在线瓦片影像；网络失败时会降级到本地程序化影像
- 在线影像会随视角变化切换可见瓦片和 zoom，而不是启动时全量拉取全球固定 zoom 瓦片
- 地球表面优先加载真实高程位移；网络失败时保留程序化地形回退，而不是完全光滑球体
- 当前视角附近会加载统一的球面 surface tile mesh，而不是只把所有在线瓦片烘到一张整球纹理
- 深入缩放时，surface tile 会提供比第四阶段更细的局部影像和地形细节
- surface tile 不可见或离开视野后会从场景中回收，而不是一直常驻整球
- surface tile 采用混合 LOD 叶子集：中心区域可细化一级，外围保持父级瓦片，不要求全屏同一 zoom
- surface tile 高程解码优先走 worker，worker 不可用时自动回退主线程解码
- 相邻或跨 LOD 的 surface tile 边缘无明显缝隙开裂（通过 skirt 抑制）
- 场景包含大气层光晕与星空背景
- 页面上至少存在 3 个可见点标记
- 页面上存在至少 1 条折线覆盖物
- 页面上存在至少 1 个填充多边形覆盖物
- 拖拽鼠标时地球会围绕地心旋转，松手后会按拖拽力度继续惯性转动并逐渐停下
- 滚轮缩放方向符合直觉：向下远离、向上靠近，且会按滚轮力度继续惯性缩放并逐渐停下
- 视角允许跨越北极或南极连续旋转
- 点击标记时右侧状态面板显示 `marker:<id>`
- 点击折线时右侧状态面板显示 `polyline:<id>`
- 点击多边形时右侧状态面板显示 `polygon:<id>`
- 点击普通球面时右侧状态面板显示经纬度
- 点击结果通过统一事件系统更新，而不是示例页自己做射线逻辑
- 窗口尺寸变化后画面仍能保持正确比例
- 相机与图层静止时不依赖持续渲染循环；惯性存在时才短时启用动画帧
- 在线瓦片采用缓存、调度器、视口 LOD 和可见区域裁剪，而不是无控制并发请求
- 统一 surface tile mesh 使用单独的 tile 生命周期管理，而不是把所有高分辨率资源永久绑定到宿主球体
- 生产构建产物包含异步 chunk，而不是所有 demo/runtime 都塞进主入口

## 自动验证

```bash
npm run test:run
npm run typecheck
npm run build
```
